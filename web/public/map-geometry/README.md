# Map geometry assets

Place locally imported `<map>.json` files in this directory.

Valve game assets are intentionally not bundled. See
`docs/map-geometry-import.md` for the provenance, conversion and validation
requirements.
