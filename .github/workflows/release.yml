# Release pipeline for RoundLab desktop.
#
# Triggered by pushing a tag of the form `v*.*.*` (e.g. `v0.2.0`). The matrix
# builds the app for macOS (arm64) and Windows (x64), bakes the parser sidecars
# into the bundle, and uploads installers to a GitHub release. The release also
# contains `latest.json`, which the in-app updater polls for new versions.
#
# Secrets required on the repo:
#   TAURI_SIGNING_PRIVATE_KEY           — contents of .tauri-keys/roundlab.key
#   TAURI_SIGNING_PRIVATE_KEY_PASSWORD  — empty string if the key has no password
#
# No Apple / Microsoft code-signing: installs on macOS require right-click →
# Open (or System Settings → Privacy → Open Anyway); installs on Windows show
# SmartScreen's "unknown publisher" warning. This is documented in the README.
name: release

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:

# tauri-action needs to create a release and upload assets. Declaring the
# permissions explicitly avoids relying on the repo-level default, which is
# read-only on GitHub Free unless flipped in Settings → Actions.
permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: macos-arm64
            os: macos-14
            rust-target: aarch64-apple-darwin
            tauri-args: "--target aarch64-apple-darwin"
          - name: windows-x64
            os: windows-latest
            rust-target: x86_64-pc-windows-msvc
            tauri-args: "--bundles nsis"

    runs-on: ${{ matrix.os }}
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust-target }}

      - name: Cache Rust builds
        uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.rust-target }}
          workspaces: |
            web/src-tauri -> target
            parser-fallback -> target

      - name: Install protoc
        uses: arduino/setup-protoc@v3

      - name: Install Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.23"
          cache-dependency-path: parser/go.sum

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Install Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: pnpm
          cache-dependency-path: web/pnpm-lock.yaml

      - name: Install frontend dependencies
        working-directory: web
        run: pnpm install --frozen-lockfile

      - name: Build parser sidecars for ${{ matrix.rust-target }}
        shell: bash
        working-directory: web/src-tauri/binaries
        env:
          TARGET: ${{ matrix.rust-target }}
          CARGO_NET_GIT_FETCH_WITH_CLI: "true"
        run: ./build-sidecar.sh

      - name: Build + release (tauri-action)
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          # `__VERSION__` is replaced by the tag name (without the leading `v`).
          tagName: ${{ github.ref_name }}
          releaseName: "RoundLab ${{ github.ref_name }}"
          releaseBody: |
            See [CHANGELOG.md](https://github.com/${{ github.repository }}/blob/main/CHANGELOG.md) for the full notes.

            **macOS**: unsigned — right-click → Open on first launch.
            **Windows**: unsigned — click "More info" → "Run anyway" in SmartScreen.
          releaseDraft: false
          prerelease: false
          projectPath: web
          args: ${{ matrix.tauri-args }}
          updaterJsonPreferNsis: true
