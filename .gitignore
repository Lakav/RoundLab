# dependencies
node_modules/
.pnp
.pnp.*

# Next.js
.next/
out/
build/
next-env.d.ts
*.tsbuildinfo

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-debug.log*

# env
.env
.env.*
!.env.example

# Desktop frontend caches
.next/

# OS / editor
.DS_Store
.idea/
.vscode/

# local agent files
.claude/

# CS2 demo inputs and generated parser outputs
*.dem
*.dem.zst
*.json.gz
desktop/data/
test.json
test.json.gz

# compiled binaries
parser/parser
parser/*.exe
parser-fallback/target/

# temp / caches
coverage/
tmp/
temp/

# Tauri updater signing keys (private key MUST stay local — .pub can be committed
# but we keep the whole folder out for simplicity; the public key is embedded in
# tauri.conf.json).
.tauri-keys/

# Tauri build artefacts
desktop/src-tauri/target/
desktop/src-tauri/gen/
desktop/src-tauri/binaries/parser-*
!desktop/src-tauri/binaries/build-sidecar.sh
